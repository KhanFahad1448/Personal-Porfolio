# Copy this file to .env and fill in your real key.
# This is ONLY used by the local dev API server (server/local-api.js).
# In production, RESEND_API_KEY is set in Netlify's dashboard instead.
RESEND_API_KEY= resend_api_key
